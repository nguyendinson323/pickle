import React from 'react';

const TestApp: React.FC = () => {
  return (
    <div style={{ padding: '20px', textAlign: 'center' }}>
      <h1>Test App - Frontend is Working!</h1>
      <p>This is a simple test to check if React is loading.</p>
    </div>
  );
};

export default TestApp;